version: '3'

services:
  laravel:
    image: helioalb/laravel:prod
    container_name: laravel
    networks:
      - laranet
  nginx:
    image: helioalb/nginx:prod
    container_name: nginx
    networks:
      - laranet
    ports:
      - 80:80
networks:
  laranet:
    driver: bridge
